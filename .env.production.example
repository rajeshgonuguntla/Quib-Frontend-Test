# Copy to .env.production for production Docker/Cloud Build builds.
# Vite loads this automatically when NODE_ENV=production (npm run build).

VITE_API_BASE_URL=https://your-backend-cloud-run-url.run.app
VITE_GOOGLE_CLIENT_ID=your-google-oauth-web-client-id.apps.googleusercontent.com
